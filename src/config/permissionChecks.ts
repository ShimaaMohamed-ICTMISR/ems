/**
 * When `true`, all client-side permission checks are bypassed:
 * PermissionRoute, Sidebar module filters, and can/canAny/has* helpers.
 * Set to `false` when roles and JWT permissions are enforced in production.
 */
export const ALL_PERMISSION_CHECKS_DISABLED = true;
